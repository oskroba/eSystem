function hello(request, response) {//	var queryStr;//	//	//	queryStr = application.getURLQuery(request.rawURL); //	//debugger;//	//	//var myCollection = ds.People.query("lastname = dubois and firstname = jules");//	//	//	//	//	//	var wynik = ds.Księgarnia.query("Nazwa = MassaKaka"); //queryStr.nazwa"); //	//	if(wynik.length = 0) { // 	 	var nowaKsiegarnia = new ds.Księgarnia({//            // create an entity in the User datastore class//            Nazwa : queryStr.nazwa,//            Adres : queryStr.adres//        });//        nowaKsiegarnia.save();     // save the user//    	return "Rekord " + queryStr.nazwa + " wstawiony";//		} //	else{//		 return "Rekord " + queryStr.nazwa + " już istnieje";//		}				 return "Rekord Swiata";}